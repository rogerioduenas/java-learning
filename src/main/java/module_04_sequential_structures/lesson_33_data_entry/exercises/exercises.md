1 - Create a feature that receives a single word provided by the user and returns that word displayed as a confirmation message.

2 - Create a feature that receives an integer provided by the user and displays a message indicating the value received.

3 - Create a feature that receives a decimal number provided by the user and displays a message with that value formatted with two decimal places.

4 - Create a feature that receives a single character provided by the user and displays a message indicating which character was entered.

5 - Create a feature that receives three values provided by the user—a word, an integer, and a decimal number—and displays a summary message containing the three values clearly identified.